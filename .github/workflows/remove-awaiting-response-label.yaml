name: "Manage Awaiting Response Label"

on:
  issue_comment:
    types: [created]

permissions:
  issues: write
  pull-requests: write

jobs:
  run:
    uses: anchore/workflows/.github/workflows/remove-awaiting-response-label.yaml@91139bea23527e94db5a1e9e9b77ad2975417930 # v0.2.0
    secrets:
      token: ${{ secrets.OSS_PROJECT_GH_TOKEN }}
